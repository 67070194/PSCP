#include "stdio.h"

int main() {
    int a = 17, b = 25, c = 50, d = 10;

    //c += ++a + b++;

    //-a++;
    //-++a;

    //a += a++;
    //a += ++a;

    //b += a++;
    //b  += ++a;

    //d = a++ + b++ + c++;
    //d = ++a + ++b + ++c;

    //a += b *= c -= 5;
    //d = a += b *= c -= 5;

    //d = a *= c / b + 5;
    //d = (a *= c) / b + 5;

    //d = b % a + c / b;
    //d = b % a++ + c / b;
    //d = b % ++a + c / b;
    //d = b % (a + c) / b;

    return 0;
}